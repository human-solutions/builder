use super::plugin::Plugin;

struct Input {
    // envs: Envs,
    plugins: Vec<Plugin>,
    // binaries: Vec<Binary>,
    // githooks: Vec<GitHook>,
}

struct Envs;

// struct Binary;

// struct GitHook;
