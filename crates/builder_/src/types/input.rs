use super::{envs::Envs, plugin::Plugin};

struct Input {
    envs: Envs,
    plugins: Vec<Plugin>,
    // binaries: Vec<Binary>,
    // githooks: Vec<GitHook>,
}

// struct Binary;

// struct GitHook;
